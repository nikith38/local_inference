{
  "name": "explainer-app",
  "version": "1.0.0",
  "description": "Simple explainer app using local LLM",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "ollama": "^0.4.4"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
} 